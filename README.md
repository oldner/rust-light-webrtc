### Simple Rust WebRTC

this is a very simple implementation of webrtc using Rust (with its advanced concepts) to see trade-offs of Rust language and deep memory management concepts.

just run
```bash
cargo run
```

and go open `localhost:8080` on your browser.
